import { GameGradeTypePipe } from './game-grade-type.pipe';

describe('GameGradeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GameGradeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
