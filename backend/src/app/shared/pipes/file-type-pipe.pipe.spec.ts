import { FileTypePipePipe } from './file-type-pipe.pipe';

describe('FileTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FileTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
