import { ExhibitUseTypePipe } from './exhibit-use-type.pipe';

describe('ExhibitUseTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ExhibitUseTypePipe();
    expect(pipe).toBeTruthy();
  });
});
